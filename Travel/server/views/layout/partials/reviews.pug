blockquote.text-center Our Clients experiences
.row.reviews
    each review in reviews
        .col-md-6.col-lg-4.mb-4
            .card
                .card-body
                    blockquote.blockquote
                        p.mb-0
                            | #{review.message}
                        footer.blockquote-footer
                            | #{review.name}